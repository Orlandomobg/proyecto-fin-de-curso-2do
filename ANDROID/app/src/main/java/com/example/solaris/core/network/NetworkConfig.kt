package com.example.solaris.core.network

object NetworkConfig {
    // Emulador Android Studio -> apunta al localhost de tu PC
    const val BASE_URL = "http://10.0.2.2:4000/"

    // Si pruebas en un móvil físico en la misma WiFi, comenta la de arriba
    // y usa la IP local de tu PC, ej:
    // const val BASE_URL = "http://192.168.1.50:4000/"
}